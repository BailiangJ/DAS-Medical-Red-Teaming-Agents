# Provider credentials used by live runs.
# Copy to an untracked .env file if you use a local .env workflow:
#   cp .env.example .env
#   set -a; source .env; set +a
# Do not commit populated credentials.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
DEEPSEEK_API_KEY=
HF_TOKEN=
